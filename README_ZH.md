> [!WARNING]
> **注意：插件仍在开发中（WIP）**
> 功能和行为可能会在不同版本之间发生变化。请谨慎使用，并在 GitHub 上报告问题。

# Auto Move by Frontmatter 插件（适用于 Obsidian）

[English](./README.md)

根据笔记中的 frontmatter 字段，自动将文件移动到指定文件夹，帮你保持知识库清晰有序！

## 功能特性

-   🎯 **多字段匹配**：规则可配置多个 frontmatter 条件
-   🔀 **灵活逻辑**：支持 AND / OR 运算符组合条件
-   ⚡ **多种触发方式**：保存、切换文件、定时扫描
-   🎨 **智能匹配**：支持精确匹配、包含、正则表达式
-   📁 **自动创建文件夹**：目标文件夹不存在时自动生成
-   🔄 **批量操作**：一次性扫描并整理所有文件
-   🎛️ **简洁配置**：直观 UI，支持文件夹建议和实时预览

## 安装方法

### 从 Obsidian 社区插件安装

> [!NOTE]
> 社区插件安装目前不可用。请使用下方的手动安装方法。

1. 打开 Obsidian 设置
2. 进入 **社区插件**，关闭安全模式
3. 点击 **浏览**，搜索 “Auto Move by Frontmatter”
4. 点击安装并启用插件

### 手动安装

1. 前往 [GitHub Releases](https://github.com/baddate/auto-move-by-frontmatter/releases) 下载最新版本
2. 解压到你的库目录 `.obsidian/plugins/auto-move-by-frontmatter/`
3. 重启 Obsidian
4. 在设置 → 社区插件中启用插件

## 使用方法

### 基本配置

1. 打开 **设置 → Auto Move by Frontmatter**
2. 启用插件
3. 点击 **Add Rule** 创建规则
4. 配置条件与目标文件夹

### 创建规则

每条规则包括：

-   **条件**：一个或多个 frontmatter 字段条件
-   **逻辑运算符**：AND（全部满足）或 OR（任意满足）
-   **目标文件夹**：符合条件的文件将移动到这里
-   **自动创建**：目标文件夹不存在时自动创建

### 示例规则

#### 示例 1：日记笔记

```yaml
Condition 1:
    Field: type
    Match Type: Exact
    Value: daily

Target Folder: Daily Notes
```

#### 示例 2：活跃项目

```yaml
Condition 1:
    Field: category
    Match Type: Exact
    Value: project

Condition 2:
    Field: status
    Match Type: Exact
    Value: active

Logic Operator: AND
Target Folder: Projects/Active
```

#### 示例 3：多个类别

```yaml
Condition 1:
    Field: category
    Match Type: Contains
    Value: work

Condition 2:
    Field: category
    Match Type: Contains
    Value: business

Logic Operator: OR
Target Folder: Professional
```

#### 示例 4：正则匹配日期标签

```yaml
Condition 1:
    Field: tags
    Match Type: Regex
    Value: \d{4}-\d{2}-\d{2}

Target Folder: Archive/Dated
```

### 匹配方式

-   **Exact（精确匹配）**：字段值需完全相同（忽略大小写）
-   **Contains（包含匹配）**：字段值需包含目标内容（忽略大小写）
-   **Regex（正则匹配）**：字段值需匹配正则表达式

### 触发条件

-   **保存时**：保存文件后自动移动（默认）
-   **切换文件时**：切换文件时自动移动
-   **定时扫描**：按设定的时间间隔扫描并移动

### 命令

-   **根据 frontmatter 移动当前文件**
-   **扫描并移动所有文件**

## Frontmatter 示例

#### 简单 frontmatter

```yaml
type: daily
status: active
```

#### 嵌套字段

```yaml
metadata:
    category: project
    priority: high
tags:
    - work
    - important
```

（可用 `metadata.category` 访问）

#### 数组字段

```yaml
tags:
    - meeting
    - project
```

插件会检查数组中任意元素是否符合条件。

## 设置说明

### 全局设置

-   启用 / 禁用插件
-   是否在保存、切换、定时触发
-   定时扫描间隔（分钟）

### 规则设置

-   启用 / 禁用单条规则
-   添加多个条件
-   设置逻辑运算符
-   选择目标文件夹（支持自动创建）

## 最佳实践

1. **先从简单规则开始**
2. **用手动命令测试规则**
3. **条件尽量明确**，避免误移
4. **注意规则顺序**（优先匹配）
5. **定期备份**，尤其是批量操作前
6. **测试正则表达式**
7. **留意通知提示**，防止意外移动

## 兼容性

-   **Obsidian v1.0.0+**
-   **移动端支持**（iOS / Android）
-   **支持同步**（Obsidian Sync / 第三方同步）

## 开发说明

```bash
git clone https://github.com/baddate/auto-move-by-frontmatter.git
cd auto-move-by-frontmatter
npm install
npm run build
npm run dev
```

项目结构：

```
auto-move-by-frontmatter/
├── src/
│   ├── main.ts              # 插件入口
│   ├── settings.ts          # 设置界面和类型
│   ├── fileMover.ts         # 文件移动逻辑
│   ├── frontmatterParser.ts # YAML 解析
│   └── fileSuggester.ts     # 文件夹建议 UI
├── styles.css               # 插件样式
├── manifest.json            # 插件清单
└── README.md
```

## 贡献方式

1. Fork 仓库
2. 新建分支 (`git checkout -b feature/AmazingFeature`)
3. 提交修改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

## 更新日志

### v1.1.0

-   ✨ 支持多字段条件
-   ✨ 支持 AND/OR 逻辑运算
-   🎨 改进 UI 层次结构
-   ♿ 增强无障碍支持
-   🔧 优化代码和性能
-   🐛 修复文件夹建议定位问题

### v1.0.0

-   🎉 初始版本
-   ⚡ 基于 frontmatter 的文件移动
-   🎯 多触发条件
-   📁 自动创建文件夹

## 协议

[MIT License](LICENSE)

## 支持

-   🐛 [问题追踪](https://github.com/baddate/auto-move-by-frontmatter/issues)
-   💬 [讨论区](https://github.com/baddate/auto-move-by-frontmatter/discussions)

## 致谢

作者：[Mercas](https://github.com/baddate)

特别感谢：

-   Obsidian 官方团队
-   Obsidian 插件开发社区
-   所有贡献者与反馈用户

---

如果你觉得这个插件有帮助，可以：

-   ⭐ 给仓库点个星
-   🐛 反馈问题
-   💡 提出新功能建议
